# Anonymised single-environment datastore fixture (acme/orders).
#
# Regression guard for S1.1 Part D: the common case is ONE logical database
# with ONE deployment surface. The refactor (Datastore → N DatabaseEndpoint)
# must NOT over-emit endpoints here — exactly one EnvironmentVariant, hence
# exactly one `:DatabaseEndpoint`.
DATABASE_URL=mysql://orders-db.acme.internal:3306/orders_production
