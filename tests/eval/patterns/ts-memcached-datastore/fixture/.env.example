# Anonymised Memcached connection fixture (acme/orders).
# Pins S1.2: the connection layer must RECOGNISE memcached (it previously did
# not — no scheme in parseDsn, no DSN key pattern, no TECH_KEYWORDS entry).
MEMCACHED_URL=memcached://cache.acme.internal:11211
