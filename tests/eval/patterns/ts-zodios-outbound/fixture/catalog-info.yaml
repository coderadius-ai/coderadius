apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ts-zodios-outbound
  description: Pattern eval fixture — Zodios OUTBOUND API detection
  annotations:
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  lifecycle: experimental
  owner: team-eval
