# Anonymised fixture: consumer-side service carrying a full AMQP DSN in the
# VALUE (s1 scheme contract) under an arbitrary key name. Credentials below
# are fixture placeholders and must never be persisted to the graph.
NOTIF_BROKER_URL=amqp://acme-svc:fixture-secret@mq.acme-internal.consul:5672/inventory
NOTIF_QUEUE_PREFIX=notifications
